import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    'kinswoman-boondocks-tactile.ngrok-free.dev',
    'chester-strengths-motorcycles-macintosh.trycloudflare.com',
  ],
};

export default nextConfig;
